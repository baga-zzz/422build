ssh-keygen -t ed25519 -C "your_email@example.com"
 eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
操作命令初始化本地仓库git init
关联远程仓库git remote add origin git@github.com:用户名/仓库名.git
添加所有更改git add .
提交更改git commit -m "你的提交说明"
推送至 GitHub  # 先删除之前错误的远程仓库地址
git remote remove origin
# 先删除之前错误的远程仓库地址
git remote remove origin

# 添加正确的 SSH 地址
git remote add origin git@github.com:baga-zzz/422build.git# 1. 提交代码（刚才因为没设置身份，commit 失败了）
git add README.md
git commit -m "first commit"

# 2. 确保本地分支叫 main
git branch -M main

# 3. 推送到 GitHub
git push -u origin main
