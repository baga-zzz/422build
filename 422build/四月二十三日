valgrind ./ex1
CFALGS=-Wall -g
all: ex1 ex2
clean :
	rm -f ex1 ex3
1. malloc()：向操作系统“租借”内存
全称：Memory Allocation（内存分配）。

作用：当你不知道需要多少内存（比如根据用户输入决定），或者希望这块内存的寿命比当前函数还要长时，你就可以用 malloc 去“堆区（Heap）”租一块空间。

怎么用：你必须告诉它你需要租多大的地方。在代码中，malloc(sizeof(struct Person)) 的意思就是：“操作系统老大，请给我分配一块正好能放下 Person 这个结构体大小的内存。”

返回值：如果租借成功，操作系统会把这块空间的**起始地址（指针）**交给你；如果内存满了借不到，它会返回一个 NULL（空指针）。

2. assert()：开发阶段的“安全检查员”
全称：断言。

作用：它用来强行设定一个“必须满足的前提条件”。如果括号里的条件为真，程序继续当无事发生；如果条件为假，程序会立刻崩溃，并在屏幕上大声报错，告诉你是在哪一行代码挂掉的。

代码里的体现：assert(who != NULL);。因为刚才说了，malloc 可能会借不到内存（返回 NULL）。如果拿到一个 NULL 指针，你还强行往里面塞数据（比如名字、年龄），程序就会发生极其严重的错误（段错误 Segmentation Fault）。所以用 assert 把关：“我断言 who 绝对不是空指针，如果是，立刻停下来！”。

注：这通常只在写代码和调试时使用，软件真正发布时可以通过设置关掉它。

3. who->：专属指针的“寻址开箱”操作符
专业叫法：箭头操作符（Arrow Operator）。

作用：用来访问结构体指针里的成员。

为什么不用 .（点操作符）？

如果你手头直接拿着一个真正的结构体（实体），比如 struct Person joe;，你想修改年龄，就用点：joe.age = 20;。

但在代码中，who 是一个指针（地址）。它只是一张写着内存门牌号的纸条，不是那个房子本身。为了修改房子里的东西，你必须先顺着地址找到房子（解引用 *who），然后再进去找年龄（点 .age）。

也就是说，严格写出来应该是 (*who).age。因为这样写太丑了，C 语言的发明者就创造了 -> 这个语法糖。who->age 等价于 (*who).age，意思是：“顺着 who 的地址找过去，把里面的 age 拿出来”。

4. free()：有借有还，再借不难
作用：把你用 malloc 租来的内存，主动还给操作系统。

为什么必须用：和 Python 或 Java 这类自带“垃圾回收保洁员”的语言不同，C 语言极其自由但也极其冷酷。你用 malloc 借的内存，如果你不用 free 还回去，这块内存就会一直被霸占着，哪怕你的程序已经不需要它了。

后果：这叫做内存泄漏（Memory Leak）。如果在一个不断循环的程序里忘了写 free，你的电脑内存会被一点点吃光，直到最后系统卡死崩溃。

代码里的体现：先 free(who->name)，再 free(who)。就像拆掉一座有保险柜的房子，你得先处理掉保险柜，再去拆房子主体。
